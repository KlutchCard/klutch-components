import React from "react"
import { KeyboardAvoidingView, ViewProps } from "react-native"

export const KKeyboardAvoidingView = (props: ViewProps) => (<KeyboardAvoidingView {...props} />)


export default KKeyboardAvoidingView
